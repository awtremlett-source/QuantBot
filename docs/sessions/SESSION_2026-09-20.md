# SESSION 2026-09-20 — TradeScout merged in (merge stages 0–2 of 6)

Operator requirement, verbatim (2026-09-18): "combine both the Manual paper
trading dashboard, and the multi-strategy trader bot into one thing. and then
i want to revise how it works."

Scope of this session: stages 0–2 only — snapshot, move in, build the wall.
No UI merging, no data-layer merging, no dependency reconciliation, and zero
change to engine behaviour.

## Stage 0 — snapshot
- QuantBot working tree clean; tagged `pre-merge-2026-09-18` at fdf1f4b.
- TradeScout working tree ALSO clean — so the commit the box anticipated
  ("Continuous dial, auto-backups, visible UI overhaul") was not needed and
  was not made. Tagged `pre-merge-2026-09-18` at 29e8e74 ("50% money ladder +
  friendlier GUI", 2026-08-21). Its GitHub remote untouched (tag is local).
- Journal + trials backed up OUTSIDE the repo to
  `C:\Users\mtrem\QuantBot-premerge-backup-2026-09-20`: the repo's own
  verified snapshot (6 tables, 82,670 rows, verify passed) PLUS a raw copy of
  the whole `data/` tree, checked file-by-file — 10 files, 82,320,834 bytes,
  every SHA-256 identical.
- Suites run separately: QuantBot 198 passed. TradeScout **131** passed, not
  the 133 the box expected — 131 collected, 131 passed, 0 skipped, 0 failed,
  so 131 is the true baseline and the number the gate was held to. (No test
  was removed; the last TradeScout commit only added tests.)
- `tools/engine_fingerprint.py` written first, then `fingerprint_before.txt`
  taken, so both sides of the clamp include the tool itself.

## Stage 1 — move in
- 54 of TradeScout's 59 TRACKED files copied (tracked-only, so no cache, venv,
  DB or stray file could come across). 5 handled specially: its `.gitignore`
  (root one covers it), root `conftest.py` and `tests/conftest.py` (folded
  into `tests/manual/conftest.py`), `requirements.txt` → `requirements-ui.txt`,
  `TradeScout.bat` → `tools_ui/TradeScout.bat`.
- Imports now resolve as `manual.scout.*` / `manual.ui.*`; 25 files rewritten.
  Nothing from TradeScout sits at repo top level — the clash-prone names `ui`,
  `data`, `config` and `tests` all live under `manual/` or `tests/manual/`.
- Paths repointed: DB → `data/manual/` (gitignored, proven with
  `git check-ignore`), shipped ticker list → `manual/assets/universe.csv`
  (NOT `manual/data/`, which the root `.gitignore`'s unanchored `data/` rule
  would have silently swallowed), settings → `manual/config.json`.

## Stage 2 — the wall
- `tests/wall/` — 18 tests over the four rules: the engine never imports
  manual/PySide6/tools_ui (ast, dynamic imports included); only
  `manual/bot_readonly.py` may name an engine artifact and only
  it + the app's own store may touch sqlite3; the engine's requirements and
  one-command installer stay headless; the banned legacy tokens appear in no
  merged path or file.
- `manual/bot_readonly.py` is the one doorway: SQLite `mode=ro` URI, so SQLite
  itself raises "attempt to write a readonly database". Proven against the
  LIVE engine database, whose hash was unchanged afterwards.
- Fail-first: each rule was demonstrated RED before green, on a sandbox COPY
  of the real trees with one deliberate violation planted per rule (the engine
  is read-only for this box, so nothing was planted in the repo). The planted
  violations are kept in `tests/museum/wall_violations/` and are asserted red
  on every run.

## Gate
- Engine fingerprint before == after:
  `25c9ec06bb13167f7cb8fde7e054251ad8243a4c9bfe84a9a1b75babbd2477ee`
  (52 files, 277,470 bytes) — reports byte-identical.
- `git diff` vs the tag: no change to any tracked engine file; the only new
  file in an engine folder is `tools/engine_fingerprint.py`.
- Tests: engine 198 (unchanged), wall 18, manual 131 — all green.
- Scheduled tasks (read-only query): QuantBot-Daily →
  `tools\run_paper_loop.bat` (next 21/09 07:30) and QuantBot-Monthly →
  `tools\run_health.bat` (next 01/10 07:45), both Ready, both targets present
  and byte-identical per the fingerprint.

## Found along the way
- The two apps run on DIFFERENT interpreters with conflicting pins (pandas
  3.0.2 vs 2.3.3, yfinance 1.4.1 vs 0.2.66). Reported, deliberately not
  resolved — a library version is part of what produced the live record.
- The operator's existing manual trade journal was NOT migrated; the copy here
  starts empty.

---

# SESSION 2026-09-20 (second sitting) — merge stage 3a: Bot tab + housekeeping

Scope: the flags QT-01 left open, then a Bot tab inside the PySide6 window.
Engine behaviour unchanged. The Tkinter panel stays exactly where it is.

## Housekeeping
- `.venv-ui` built from `requirements-ui.txt` (PySide6 6.11.1, pandas 2.3.3,
  yfinance 0.2.66, mplfinance, matplotlib 3.10.7, numpy 2.3.4, pytest 9.1.1).
  The launcher now REQUIRES it and prints how to create it if absent — it no
  longer falls back to a global Python whose versions drift.
- LOCKED: two environments, permanently, with two doorways. Recorded in
  STATE.md and MERGE_PLAN.md. Version alignment deferred to the 2027-07 refit.
- Old folder retired: `Documents\TradeScout\TradeScout.bat` now prints where
  the app went and exits, plus MOVED.txt. Local commit 60b7142 in that repo,
  deliberately NOT pushed; the desktop shortcut was not touched.
- Journal migration STOPPED on its own rule: the operator's `trades` table
  declares `shares INTEGER`, the code declares `shares REAL` (TradeScout commit
  4e15cf4 changed the CREATE TABLE; `IF NOT EXISTS` never altered the existing
  table). Its two trades are both open, 100 shares each. The cache was backed
  up and verified first (1 file, 12,075,008 bytes, SHA-256 matched).
- Lint: 16 ruff findings fixed. One was NOT cosmetic — `_refresh_done` read a
  bare `cfg` that only exists as an `__init__` parameter, so every completed
  data refresh raised NameError and the live-quote timer never started. Ruff's
  F821 found it; a fail-first test shipped with the fix (proven red first).
- mypy: the ONE permitted engine-config edit, inside `[tool.mypy]` only. 623
  errors -> 0 reported: manual/ dropped to mypy's default level, PySide6 and
  mplfinance marked stub-less (they are deliberately absent from `.venv`), and
  52 genuine errors in 5 inherited modules recorded in `docs/merge/TYPING_DEBT.md`
  with counts per file. The two doorways are held to full `--strict` and pass.

## The Bot tab
- `manual/bot_readonly.py` gained short-lived connections (`engine_db()` context
  manager, 2s lock timeout) and `read_snapshot()`. Cash is DERIVED from the
  engine's own mark (equity − shares × close) rather than copying the engine's
  starting-capital constant — a copied constant is a second source of truth.
- `manual/bot_governance.py` is new and is the only way the window launches
  anything: a fixed allow-list of exactly the panel's six commands, the ENGINE's
  interpreter, the repo root as working directory, fixed argv, never
  `shell=True`, one at a time, every press logged to
  `data/manual/governance_log.jsonl`.
- `manual/ui/bot_tab.py` shows the figures in plain words with an age on each,
  the seven buttons with the panel's labels, and a header that goes red when a
  completed trading day has gone unprocessed. It starts NOTHING on open: the
  meters box stays empty until "Refresh Status" is pressed, because a window
  that launches engine commands by itself is a window that can surprise you.
- Stronger than the panel where it matters: the loop button asks for
  confirmation (the panel does not), and closing the window while a command runs
  is BLOCKED rather than orphaning a process mid-write.

## Gate
- Fingerprint: 52 files, all 48 engine CODE files byte-identical; `pyproject.toml`
  the only change, and only inside `[tool.mypy]`.
  `25c9ec06…2477ee` -> `4add56ec…743b6`.
- Tests: engine 198 unchanged · wall 18 -> 44 · manual 131 -> 147.
  `ruff check .` and `mypy --strict .` both green.
- Scheduled tasks Ready, both `.bat` targets byte-identical.
- Offscreen launch: all 7 screens open and close clean; the Bot tab's equity,
  share count and trial count match a direct read of the journal; nothing was
  pressed (no governance log was created).

## Found along the way
- The bot is 31 completed trading days behind (newest bar 2026-08-06). The
  laptop has been off; the loop is catch-up-safe. The tab reads RED, correctly.
- Reading a WAL database read-only can create `-shm`/`-wal` beside it. The
  engine's own monitors use the identical `mode=ro` URI and do the same, the
  `-wal` is 0 bytes, the database content hash is unchanged and no lock
  survives. The test was rewritten to say this rather than to pass quietly on a
  non-WAL fixture.
- Two "Windows fatal exception: access violation" lines from the manual suite
  are PRE-EXISTING: the untouched original repo produces them too.
