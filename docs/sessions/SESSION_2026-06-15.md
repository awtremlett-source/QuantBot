# SESSION 2026-06-15 — First Light (setup · recon · decisions)

Mentor: Lead Quant Strategy Architect (persona) under the epistemic rider.
Operator: complete beginner (assumed until shown otherwise).

## Founding directive (requirements-verbatim law, §12)
The founding directive is the operator's pasted **QUANTBOT KICKSTART** document
(first message, 2026-06-15).
- [ ] OPEN: capture the full document verbatim to `docs/FOUNDING_DIRECTIVE.md`
      next session. (Verbatim source is preserved in the chat transcript; flagged
      OPEN so it is not treated as done.)

## Environment recon (DOCUMENTED — observed this session)
- OS: Windows 10 Home 19045. Shells: PowerShell (primary) + Bash available.
- Python 3.13.7 (doc asked 3.11+ — newer is fine). git 2.45.0. pip 25.2.
- Repo root: `c:\Users\mtrem\TRADING`. No git repo existed before → nothing leaked.
- Disk: C: 437 GB free of 1887 GB → ample; the Stooq 5-min pack fits easily.
- Clock: **NOT synchronized** (Leap Indicator 3; last sync ~26h ago; root
  dispersion 8.2s). Scar #20. Fix queued for operator: elevated `w32tm /resync
  /force`; daily auto-sync task to follow. Does not block today.

## Prior work found (NOT created by me — preserved untouched, reviewed)
1. `nvda-ema-t212/t212_demo.py` — clean documented T212 demo REST client
   (account/portfolio/instruments/orders). REUSABLE.
   - FLAG (INFERRED): uses Basic base64(KEY:SECRET). Current official T212 public
     API uses a single api-key header. Verify vs official docs before any order.
2. `nvda-ema-t212/strategy_nvda_ema5.py` — EMA(5) on 5-min NVDA bars (yfinance) →
   T212 market orders. Out of scope (§17), but a plumbing reference. Uses
   `iloc[-2]` (no same-bar look-ahead — good).
3. `nvda-ema-t212/requirements.txt` — STALE: lists alpaca-trade-api + ib-insync;
   code actually uses requests + yfinance.
4. `TradingResearch/paper_portfolio_system.py` — 2,134-line teaching-grade paper
   portfolio: SMA200 trend + momentum + regime + vol-targeting + WFO + cost model
   + trailing/time stops + correlation cap + SMTP reports. Close to §8 CORE+EXPLORER.
   - FLAG (INFERRED): WFO grid-search has NO trial-counting / deflation / purged CV
     → the exact overfitting risk §7 exists to catch. Reference only; must pass the
     (not-yet-built) §7 firewall before promotion.

## Decisions this session
- D1: Repo root = `c:\Users\mtrem\TRADING` (`git init -b main`). Non-destructive,
      nothing committed, reversible.
- D2 (PENDING operator): treatment of prior work — recommend preserve in `legacy/`,
      build fresh, mine the T212 client. Asked via DECISION REQUESTED.

## Safety rails up this session (§2b)
- [x] git init -b main (repo root = TRADING)
- [x] .gitignore — verified: `.env` ignored (rule L7), `.env.example` kept (L15)
- [x] .env.example (names only — Scar #1)
- [x] docs/SCARS.md (§13 verbatim, 21 laws)
- [x] clock-sync + free-disk recon (see above)
- [ ] CLAUDE.md / STATE.md / GRAND_TODO.md / docs/EDUCATION.md#1 — next session
- [ ] Python venv + requirements.txt — next session
- [ ] Stooq bootstrap PROPOSE doc — next session (gated on machine answer)
- [ ] Validation-firewall skeleton — next session (precedes all strategies)

## Onboarding (defaults stand until answered, §16)
- capital/currency — ASKED (default £10,000 GBP)
- machine always-on? — ASKED (disk already known ample)
- universe size — ASKED (default focused ~100 liquid names)
- prior-work handling — ASKED (default preserve-as-reference)
- hours/week — default 3–5 (confirm anytime)
- Telegram OK — default yes (FIRST LIGHT milestone)
- prior experience — default none (Lesson 0 starts from zero)

## Next session (resume here)
1. If operator answered: write FOUNDING_DIRECTIVE.md verbatim; CLAUDE.md; STATE.md;
   GRAND_TODO.md; EDUCATION.md entry #1 ("How this system makes & loses money").
2. Create venv + requirements.txt (pandas, numpy, pyarrow, requests, scikit-learn,
   statsmodels, matplotlib, pytest, hypothesis, ruff, mypy).
3. Stooq PROPOSE doc (enumerate the live db/h page; sizes/counts/disk).
4. Begin the §7 validation-firewall skeleton (before any strategy).
