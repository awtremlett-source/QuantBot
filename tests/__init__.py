"""Test suite for QuantBot.

This file makes ``tests`` a package so test modules import as
``tests.<area>.<module>`` -- which keeps a test package like ``tests/data_store``
from shadowing the real top-level ``data_store`` package on sys.path.
"""
